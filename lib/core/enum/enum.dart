enum HomeLayoutStatesEnum {
  homeLayoutInitState,
  changeBottomNavState,
}
enum TabType { all, unread }
enum LearningStatus { all, inProgress ,Done}
enum ToastStates { congrats, error, info, warning }
enum RequestStates { initial, loading, loaded, error }
